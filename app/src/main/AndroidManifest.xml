<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.learn.mdessa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".recyclerView.RecyclerView_main"></activity>
        <activity android:name=".moving_data_between_activity.sec_activity_data" />
        <activity android:name=".alert_Dialogue" />
        <activity android:name=".lifeCycleActivity" />
        <activity android:name=".calendar" />
        <activity android:name=".rating_bar" />
        <activity android:name=".switch_button" />
        <activity android:name=".checkBox" />
        <activity android:name=".toggle_button" />
        <activity android:name=".radio_button" />
        <activity android:name=".MainActivity" />
        <activity android:name=".moving_data_between_activity.main_actvity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>